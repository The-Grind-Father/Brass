#pragma once
#include <cstdint>
#include <string>

// enum class TARGET { SELF, ENEMY, ALLY, ALLALLIES, ALLENEMIES, ALL };
// enum class SCALER { NONE, ATTACK, DEFENSE, AGILITY, ACCURACY, ENERGY, LUCK };
// enum PWTYPE {HP, EP, AMMO};
// struct Move {
//     Move(std::string name, uint32_t cost, TARGET target, type pw, uint32_t effect, SCALER s) : 
//     Name(name), Cost(cost), PW(pw), Target(target), Effect(effect), Scaler(s) {};
//     std::string Name = "no name";
//     uint32_t Cost = cost; // what is the cost
//     PWTYPE PW = HP;
//     TARGET Target = TARGET::SELF; // who is the target
//     uint32_t Effect = 1;
//     SCALER Scaler = SCALER::NONE;
// };

